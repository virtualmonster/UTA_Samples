steps:
  - with:
      context: FOO
      steps:
        - do: open-app.dtx.yaml
  - with:
      context: BAR
      steps:
        - log: other thing
  - with:
      context: FOO
      steps:
        - press: {}
